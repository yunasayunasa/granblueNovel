; macro.ks
[macro name="clearname"]
    ; ★★★ x と y パラメータを追加 ★★★
    ; layer, color, size, bold などの属性も、元の定義と合わせておくとより確実ですが、
    ; text="" で内容を消すだけなら、最低限 name, x, y があれば動作するはずです。
    ; ここでは、元の定義に合わせてみます。
    [ptext name="chara_name_area" layer="message0" x="40" y="300" text="" size="20" bold="true" color="white"BRIEF]
[endmacro]